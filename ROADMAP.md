# 项目复盘与路线图

> 面向目标：2027 年 3 月暑期实习投递（2028 届）。核心诉求是「有一段能写进简历、能扛住追问的实习/项目经历」。
> 复审时间：2026-09-18。本文档是事实基线 + 优先级 + 依赖关系，后续每完成一项就更新一次。

---

## 一、项目定位

**不是「旅游网页」，是「LLM + 组合优化的行程调度系统」。**

技术主线（面试要讲的就是这条）：

```
非结构化攻略文本
   │  LLM 结构化抽取（extractor）
   ▼
景点候选（名称/停留时长/偏好分/开放时间）
   │  实体对齐（aligner，高德 POI 检索 + 类型过滤 + 归一化匹配）
   ▼
带地理坐标的可求解实例
   │  OPTW 求解（solver：贪心构造 + 2-opt + 跨日搬运）
   ▼
多日行程（每天时间窗 / 预算 / 住宿锚点往返）
   │  约束校验（constraint_check，独立复核而非信任求解器）
   ▼
可直接执行的行程 + 地图 + 详情
```

差异化在于**三段都不是调 API 就完事**：抽取要做归一化与容错解析，对齐要做类型过滤与大小写/后缀归一，求解要处理时间窗 + 每日返回约束 + 预算。另有一条 LLM 对话编辑链路（多轮记忆、结构化指令、定点插入、住宿锚点）。

---

## 二、现状事实（2026-09-18 实测）

| 项 | 数值 |
|----|------|
| 后端规模 | 2,766 行 / 15 个模块 |
| HTTP 接口 | 18 个 |
| 单元测试 | 29 个（`pytest`，CI 全绿） |
| 前端 | `static/index.html` 1,109 行 / 56 KB（单文件，无构建） |
| 提交数 | 58（今日 20+ 次迭代） |
| 排期对照实验 | 50 组程序生成场景 × 3 个方案（结果已在 `eval_results.json`） |
| 对齐标注集 | 20 条（`eval_set.json`） |
| 缓存与限频 | 高德通勤矩阵三级缓存（内存 → 磁盘 → API，0.35s 限频） |
| 模型配置 | 主模型 DeepSeek `deepseek-chat`（抽取）；轻任务通道 GLM `glm-4-flash-250414`（免费，意图解析 + 评价生成） |

**已有的排期对照实验数据**（这是本项目最有价值的存量资产）：

| 指标 | 朴素基线 | 本项目求解器 | LLM 直接排期 |
|------|---------|-------------|-------------|
| 冲突/计划 | 0.38 | **0** | 0.06 |
| 无冲突率 | 62% | **100%** | 96% |
| 通勤占比 | 0.235 | **0.131** | 0.133 |
| 预算违规次数 | 12 | **0** | 8 |
| 质量分 | 0.857 | 0.915 | 0.918 |

结论：**LLM 直接排期的质量分甚至略高（0.918 > 0.915），但它违规 8 次、冲突 0.06**；本项目求解器 0 违规、0 冲突，通勤占比比朴素基线低 44%。
→ 这组数据正面回答了「为什么不能直接问 LLM 排行程」，是混合架构（LLM 负责理解、求解器负责合规）的实证依据。

---

## 三、能力评估（10 分制）

| 维度 | 现状 | 门槛 | 差距所在 |
|------|------|------|---------|
| 产品体验 | 8.5 | 7 | 已超标：双页结构、日历、地图分色、详情卡、导航、收藏、历史、酒店选择器 |
| 工程规范 | 7 | 8 | 有测试与 CI，但无 lint/类型检查、依赖未锁版本、README 过时 |
| 数据与评测 | 7 | 9 | 有对照实验，但换模型后未复测、标注集仅 20 条、无实验报告 |
| 算法与建模 | 6 | 9 | 有建模与启发式，**但没有最优解参照（无 gap）** |
| 系统工程 | 6 | 8 | 文件存储 + 内存任务 + 无鉴权 + 无日志 + 未部署 |

**一句话结论：短板在深度，不在宽度。继续打磨 UI 的边际收益已经很低。**

---

## 四、问题清单（按严重度排序）

### A 级｜影响「能不能讲清楚」

| # | 问题 | 证据 | 影响 | 修法 | 验收 |
|---|------|------|------|------|------|
| A1 | 没有最优解参照，无法回答「你的解离最优多远」 | 求解器只有贪心 + 2-opt | 面试「算法部分」只能说「我写了个启发式」 | 引入 OR-Tools CP-SAT 建同款模型（时间窗 + 住宿锚点 + 预算），同场景跑对照，输出 gap | `solver_cpsat.py` + `eval_gap.json`，50 场景 gap ≤ 3% |
| A2 | 换模型（DeepSeek→GLM）后从未复测质量 | `eval_results.json` 为换前数据 | 免费模型的引入缺数据支撑，被问即空 | 重跑 `evaluation.py --n 50 --with-llm` 与 `eval_aligner.py` | 新旧对照表进 README |
| A3 | 对齐标注集只有 20 条 | `eval_set.json` = 20 | F1 数字统计意义弱 | 扩到 100~200 条（覆盖同名景点、别名、连锁店等难例） | F1 + 转人工率 + 难例分布 |
| A4 | README 停留在 v0.5 / 19 测试 | 徽章与正文过时 | 别人（含面试官）看到的是旧项目 | 重写：架构图、量化结果、快速上手、设计权衡 | README 反映当前 29 测试与最新数据 |
| A5 | 无实验报告，量化资产埋在 JSON 里 | `eval_results.json` 无人阅读 | 最有价值的证据链无法呈现 | 写 `docs/experiments.md`：实验设置、三方案对比、结论与局限 | 一页纸可讲 |

### B 级｜影响「可信 / 可用」

| # | 问题 | 证据 | 影响 | 修法 | 验收 |
|---|------|------|------|------|------|
| B1 | 无持久化数据库，规划存 JSON 文件 | `data/plans/*.json` + 软删除 | 「多用户/并发/事务」三问全空 | SQLite + SQLAlchemy（`users` / `plans` / `favorites` 三表） | 迁移脚本 + 数据层测试 |
| B2 | 无用户体系，收藏与历史是全局的 | `/favorites` 无 user 维度 | 同上，且无法演示多用户 | 轻量鉴权（令牌或会话）+ `user_id` 贯穿 | 两账号数据隔离可演示 |
| B3 | 任务只进不出，内存无上限 | `MANAGER._tasks` 无淘汰 | 长跑必崩，压测会暴露 | 完成任务 TTL 淘汰 + 上限保护 | 压测 500 任务后内存平稳 |
| B4 | 前端 32 处 `innerHTML` 直插 LLM/高德返回内容 | `static/index.html` | XSS 面（LLM 输出可含标签） | 统一 `escapeHtml()` 或改 `textContent` 渲染 | 注入用例不再执行 |
| B5 | 无日志体系，只有内存进度 | 全项目无 `logging` | 线上问题不可追 | `logging` 结构化日志 + 请求 ID | 日志含请求链路 |
| B6 | LLM/高德调用无重试；`extractor` 连超时都没有 | 审计结果 | 网络抖动直接失败 | 统一超时 + 指数退避重试（tenacity） | 断网注入测试通过 |
| B7 | 依赖未锁版本、缺开发依赖 | `requirements.txt` 全 `>=` | 部署不可复现 | 拆 `requirements.txt` / `requirements-dev.txt` 并锁版本 | 全新环境一键起 |
| B8 | 未部署，简历只能放仓库链接 | 无 Dockerfile / 无线上地址 | 转化率明显低于「可点开」 | Dockerfile + 单机部署（Render/Fly.io） | 公网链接可访问 |

### C 级｜锦上添花

| # | 问题 | 修法 |
|---|------|------|
| C1 | 前端单文件 1,109 行，难维护 | 拆 `api.js` / `map.js` / `views/*.js`（原生 ESM，无需构建） |
| C2 | 测试集中在纯函数，无接口级集成测试 | 加 `test_api.py`（httpx + 临时目录） |
| C3 | CI 只跑测试 | 加 `ruff` + `mypy` + 前端语法检查 |
| C4 | 只有西安数据 | 增加成都/北京 demo 数据证明非硬编码 |
| C5 | 无分享长图 | HTML → 截图或分享页 |
| C6 | 软删除无清理策略 | 定期归档或提供「永久删除」 |

---

## 五、路线图（含依赖关系）

```
P0 解锁「能讲」
  A4 README 重写 ──┐
  A5 实验报告    ──┼──> 依赖 A2 复测数据 ──> 依赖 A1 CP-SAT 结果（gap 章节）
  A2 复测        ───┤
  A3 标注集扩充  ───┤
  A1 CP-SAT 对照 ───┘

P1 解锁「可信可用」（可与 P0 并行一部分）
  B7 依赖锁定 ─> B8 部署上线 ─> B3 任务淘汰（压测在部署环境做）
  B1 数据库 ──> B2 用户体系
  B4 XSS 修复 ─> B5 日志 ─> B6 重试     （独立，可穿插）

P2 加分
  C1 前端拆分（在加新页面前做，否则技术债滚大）
  C3 CI 增强、C2 接口测试、C4 多城市

P3 可选
  C5 分享长图、C6 归档策略

不做：多人协同、天气接入、支付、纯动效堆料
```

### 批次建议（每批 1~2 次会话，产出可验收）

**批次 1｜把已有资产变成可读结果（最高性价比）**
1. A4 README 重写（架构 + 量化结果 + 上手）
2. A5 实验报告初版（用现有 50 场景数据）
3. A2 复测（DeepSeek vs GLM 选型表，含延迟/成本/格式遵从性）
> 验收：外人只看 README 就能理解项目价值与技术判断。

**批次 2｜补算法深度**
4. A1 CP-SAT 对照 + gap 分析（含「何时用启发式、何时用精确解」的结论）
> 验收：50 场景 gap 表；能口头解释建模差异与取舍。

**批次 3｜补评测严谨性**
5. A3 标注集扩到 100~200 条 + F1 + 转人工率
> 验收：难例分布 + 指标 + 失败案例分析。

**批次 4｜工程化与上线**
6. B7 依赖锁定 → B1 数据库 → B2 用户体系 → B8 部署
> 验收：公网链接 + 两账号数据隔离演示。

**批次 5｜健壮性收口**
7. B4 XSS → B5 日志 → B6 重试 → B3 任务淘汰 + 压测
> 验收：压测报告 + 故障注入测试。

**批次 6｜前端与测试债**
8. C1 拆分、C2 接口测试、C3 CI 增强

---

## 六、面试资产包（现在就能用）

### 简历项目描述（一版草稿）

> **AI 行程规划系统**（Python / FastAPI / LLM / 组合优化，个人项目）
> 设计并实现从非结构化攻略到可执行多日行程的完整链路：LLM 结构化抽取 → 高德实体对齐 → 带时间窗与住宿锚点约束的路线调度 → 独立约束校验。
> · 自研启发式求解器（贪心构造 + 2-opt + 跨日搬运），50 组场景对照实验中 **100% 无冲突、0 预算违规**，通勤占比 0.131（朴素基线 0.235），与 OR-Tools CP-SAT 最优解 gap ≤ __%（待补）
> · 实证「LLM 直接排期质量分接近（0.918 vs 0.915）但违规 8 次」，据此确立 LLM 理解 + 求解器合规的混合架构
> · 高德通勤矩阵三级缓存（内存/磁盘/API）与限频控制，API 调用量下降 __%（待补）
> · 29 个单元测试 + GitHub Actions CI；轻任务模型通道接入免费模型，评价生成零成本

### 三个可讲的调试故事（都是真的）

1. **两套通勤口径不一致**：定点插入用 haversine 估算（兵马俑→永兴坊估 101 分钟），基准行程用高德真实数据（43 分钟），导致可行方案被判不可行 → 统一注入同一通勤函数。
2. **批量删除上限**：删 10 条成功、14 条报 500，定位到执行环境的安全删除守卫拦截物理删除 → 改为软删除（标记位），顺带获得误删可恢复能力。
3. **多轮指代记忆**：「设汉庭」→ 下一句「换到西安站附近」能正确指代住宿 → 靠编辑链路的 `chat_history` 跨任务传递。

### 预判追问与答案要点

| 追问 | 答题要点 |
|------|---------|
| 为什么用启发式不用精确解？ | 讲规模增长下的求解时间；讲 CP-SAT 对照实验的 gap 与耗时曲线 |
| 如何保证约束不被破坏？ | 求解器内每次插入都做时间线可行性校验 + 独立的 `constraint_check` 二次复核 |
| LLM 输出不稳定怎么办？ | 容错 JSON 解析 + 归一化 + 类型过滤 + 转人工（`needs_review`），并用 F1 量化 |
| 缓存怎么设计？ | 三级、命中率统计、限频 0.35s、方向敏感性（来回时长不同） |
| 为什么要独立校验？ | 求解器可能被优化步骤破坏不变量（曾出现 relocate 漏传 hotel 参数导致输出不可行） |
| 怎么做多用户？ | 讲表结构与隔离；讲内存任务淘汰与 TTL |
| 扩展新城市要改什么？ | 只换 demo 数据与 city 参数（已参数化）；讲对齐器的类型过滤可复用 |
| 你最大的技术取舍是什么？ | 「返回时间约束」让远线必须早去早回，宁可少排景点也不生成不可执行行程 |

---

## 七、命令备忘

```bash
# 启动
cd backend && python -m uvicorn main:app --port 8000
# 测试
cd backend && python -m pytest tests/ -q
# 排期对照实验（朴素 vs 求解器 vs LLM）
cd backend && python evaluation.py --n 50 --with-llm
# 对齐评测（F1 / 转人工率）
cd backend && python eval_aligner.py
```

---

## 八、更新记录

- 2026-09-18 首版：事实基线、五维评估、A/B/C 问题清单、六批路线图、面试资产包。
