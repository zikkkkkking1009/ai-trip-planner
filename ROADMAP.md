# 项目复盘与路线图

> 面向目标：2027 年 3 月暑期实习投递（2028 届）。核心诉求是「有一段能写进简历、能扛住追问的实习/项目经历」。
> 复审时间：2026-09-18。本文档是事实基线 + 优先级 + 依赖关系，后续每完成一项就更新一次。

---

## 一、项目定位

**不是「旅游网页」，是「LLM + 组合优化的行程调度系统」。**

技术主线（面试要讲的就是这条）：

```
非结构化攻略文本
   │  LLM 结构化抽取（extractor）
   ▼
景点候选（名称/停留时长/偏好分/开放时间）
   │  实体对齐（aligner，高德 POI 检索 + 类型过滤 + 归一化匹配）
   ▼
带地理坐标的可求解实例
   │  OPTW 求解（solver：贪心构造 + 2-opt + 跨日搬运）
   ▼
多日行程（每天时间窗 / 预算 / 住宿锚点往返）
   │  约束校验（constraint_check，独立复核而非信任求解器）
   ▼
可直接执行的行程 + 地图 + 详情
```

差异化在于**三段都不是调 API 就完事**：抽取要做归一化与容错解析，对齐要做类型过滤与大小写/后缀归一，求解要处理时间窗 + 每日返回约束 + 预算。另有一条 LLM 对话编辑链路（多轮记忆、结构化指令、定点插入、住宿锚点）。

---

## 二、现状事实（2026-09-18 实测）

| 项 | 数值（**2026-09-23 复核**，此前为 09-18 的旧值） |
|----|------|
| 后端规模 | Python **48 个文件 / 7,745 行**（含评测与实验脚本） |
| 接口 | **22 个**（10 GET / 10 POST / 1 DELETE / 1 WebSocket），含 `/cities`、`/plan/simulate`、`/plan/review` |
| 单元测试 | **195 个**（`pytest`，CI 全绿；实测 `pytest tests/ -q` = 195 passed / 1 skipped） |
| 前端 | `static/index.html` **2,241 行**（单文件，无构建、无 CDN） ⚠️ 09-18 时是 1,150 行，两个月不到翻倍 |
| 提交数 | **80 次**（`git rev-list --count HEAD`） |
| 排期对照实验 | 50 组程序生成场景 × 3 个方案（`eval_results.json`）；CP-SAT gap 均值 −0.03%、≤3% 覆盖 100%（`eval_gap.json`） |
| 对齐标注集 | **22 条**（`eval_set.json`），**F1 100% / 转人工率 15%**（A7 后 + A8 补 2 条难例） |
| 多城市数据 | **25 城 254 个景点**（西安 14 手写 + 24 城 240 条脚本抓取），坐标全部来自高德真实抓取 |
| 缓存与限频 | 高德通勤矩阵三级缓存（内存 → 磁盘 → API，0.35s 限频）；实体对齐 POI 缓存 + LLM 仲裁缓存 |
| 模型配置 | 主模型 DeepSeek `deepseek-chat`（抽取 / 城市识别 / 对齐仲裁）；轻任务通道 GLM `glm-4-flash-250414`（后台评价生成） |

**已有的排期对照实验数据**（这是本项目最有价值的存量资产）：

| 指标 | 朴素基线 | 本项目求解器 | LLM 直接排期 |
|------|---------|-------------|-------------|
| 冲突/计划 | 0.38 | **0** | 0.06 |
| 无冲突率 | 62% | **100%** | 96% |
| 通勤占比 | 0.235 | **0.131** | 0.133 |
| 预算违规次数 | 12 | **0** | 8 |
| 质量分 | 0.857 | 0.915 | 0.918 |

结论：**LLM 直接排期的质量分甚至略高（0.918 > 0.915），但它违规 8 次、冲突 0.06**；本项目求解器 0 违规、0 冲突，通勤占比比朴素基线低 44%。
→ 这组数据正面回答了「为什么不能直接问 LLM 排行程」，是混合架构（LLM 负责理解、求解器负责合规）的实证依据。

---

## 三、能力评估（10 分制）

| 维度 | 现状 | 门槛 | 差距所在 |
|------|------|------|---------|
| 产品体验 | 8.5 | 7 | 已超标，且不该再加（继续堆 UI 的边际收益为负）。**前端已涨到 2,241 行**，C1 拆分从"锦上添花"变成"再不拆就要失控" |
| 工程规范 | 8.5 | 8 | 依赖锁定 + **195 测试** + CI 五道门 + 前端静态检查与 jsdom 冒烟 + **五维健壮性审计**；仍缺 ruff / mypy（C3） |
| 数据与评测 | 9 | 9 | 实验报告、选型复测、F1 100%、gap 对照齐备；标注集仍只 22 条（A3） |
| 算法与建模 | 9.5 | 9 | CP-SAT gap 均值 −0.03%、多起点随机重启、独立约束校验、稳健性模拟与稳健排程（鲁棒优化） |
| 系统工程 | 7 | 8 | 有日志 / 重试 / 任务淘汰 / 软删除；**仍缺持久化数据库与用户体系（B1/B2）、未部署（B8）** |
| **文档一致性** | **5** | **8** | ⚠️ **2026-09-23 新增此维度**：README 更新日志停在 v1.2（09-19），**13 个已完成功能未写入**（含分享长图、AI 总评、小舟助手、稳健性模拟……）；测试徽章写 84 实际 195；简历草稿写 51。**这是当前最便宜、收益最高的一块短板** |

**一句话结论：五维里只剩「系统工程」未达门槛，而它恰好是面试高频考点。**
**但 2026-09-23 要补一句更尖锐的：文档一致性已经掉到 5 分——项目做的事远比它对外宣称的多，这是白白丢分。**

---

## 四、问题清单（按严重度排序）

### A 级｜影响「能不能讲清楚」

| # | 问题 | 证据 | 影响 | 修法 | 验收 |
|---|------|------|------|------|------|
| ✅ A1 | ~~没有最优解参照~~ **已完成（2026-09-18）** | `solver_cpsat.py` + `eval_gap.py` + `eval_gap.json` | 实测 gap：均值 **7.57%**、中位 5.9%、最大 24.34%、≤3% 占 46%；宽松实例 gap 0%，紧张实例（13 景点/2 天）差 21~24%。**原定「≤3%」目标不成立，据实记录**；瓶颈在"景点选择"而非"排序" | 已完成；后续改进方向见实验报告（下界约束已实现，暖启动无收益） |
| ✅ A2 | ~~换模型（DeepSeek→GLM）后从未复测质量~~ **已完成** | `bench_models.py` 多轮重复基准 + `evaluation.py` 复测 | 实测：抽取任务主模型 1.88s vs 免费 GLM 12.12s（慢 6.4 倍）→ 确立「交互路径用主模型、后台评价用免费模型」的路由 | 已完成（数据进 `docs/experiments.md` 实验三） |
| A3 | 对齐标注集只有 22 条 | `eval_set.json` = 22（A8 补了 2 条难例） | F1 数字统计意义弱 | 扩到 100~200 条（覆盖同名景点、别名、连锁店等难例） | F1 + 转人工率 + 难例分布 |
| ✅ A4 | ~~README 停留在 v0.5 / 19 测试~~ **已完成** | 徽章与正文已同步 | — | — | ✅ README 含架构、量化结果、快速上手、部署、合规；徽章测试数与正文一致 |
| ✅ A5 | ~~无实验报告~~ **已完成** | `docs/experiments.md` | — | — | ✅ 四个实验（三方对照 / 对齐 F1 / 模型选型 / CP-SAT gap）+ 失败案例 + 改进尝试（含负结果） |

### B 级｜影响「可信 / 可用」

| # | 问题 | 证据 | 影响 | 修法 | 验收 |
|---|------|------|------|------|------|
| B1 | 无持久化数据库，规划存 JSON 文件 | `data/plans/*.json` + 软删除 | 「多用户/并发/事务」三问全空 | SQLite + SQLAlchemy（`users` / `plans` / `favorites` 三表） | 迁移脚本 + 数据层测试 |
| B2 | 无用户体系，收藏与历史是全局的 | `/favorites` 无 user 维度 | 同上，且无法演示多用户 | 轻量鉴权（令牌或会话）+ `user_id` 贯穿 | 两账号数据隔离可演示 |
| ✅ B3 | ~~任务只进不出~~ **已完成** | `TaskManager` 容量 200 + TTL 2h，运行中任务不淘汰 | — | 惰性清理，`/health` 暴露 `in_memory/evicted_total` | ✅ 测试覆盖容量与 TTL 两条路径 |
| 🟡 B4 | `innerHTML` 直插外部内容 | `static/index.html` | XSS 面 | 已加 `esc()` 并覆盖 **26 处**外部来源文本（景点名/评价/地址/酒店/历史预览等） | 剩余：`onclick="fn('名字')"` 内联处理器仍是 JS 注入面，需改 `data-*` + 事件委托（已登记） |
| ✅ B5 | ~~无日志体系~~ **已完成** | `logging_setup.py` + 中间件 | — | 标准库 dictConfig，`LOG_LEVEL`/`LOG_FILE` 可控，请求 ID 贯穿 | ✅ `/health` 暴露任务表；日志含 request_id |
| ✅ B6 | ~~无重试 / extractor 无超时~~ **已完成** | `reliability.py`（标准库实现，未引入 tenacity） | — | 只重试可重试错误（超时/429/5xx），指数退避 + 抖动，重试写日志 | ✅ LLM 超时 30s、高德 8s，均带重试 |
| ✅ B7 | ~~依赖未锁版本~~ **已完成** | 三个文件：`requirements.txt`（运行时锁定）/`-dev`（pytest+httpx）/`-eval`（ortools 等） | — | CI 已改为安装锁定版 | ✅ 全部 `==` 精确版本 |
| 🟡 B8 | 未部署 | **Dockerfile + docker-compose.yml + .dockerignore 已就绪**（密钥不进镜像、data 挂卷、含 healthcheck） | 还差在平台侧创建服务（需要你的账号） | Render / Fly.io 部署，README 补链接 | 公网链接可完成一次规划 |

### C 级｜锦上添花

| # | 问题 | 修法 |
|---|------|------|
| C1 | 前端单文件 **2,241 行**（09-18 是 1,109 行，已翻倍），难维护 ⚠️ 优先级上调 | 拆 `api.js` / `map.js` / `views/*.js`（原生 ESM，无需构建） |
| C2 | 测试集中在纯函数，无接口级集成测试 | 加 `test_api.py`（httpx + 临时目录） |
| C3 | CI 只跑测试 | 加 `ruff` + `mypy` + 前端语法检查 |
| ✅ C4 | ~~只有西安数据~~ **已完成（2026-09-19），且**升级为 A 级：判定为「可用性分水岭」而非锦上添花——前端 4 处把 city 写死、抽取兜底坐标写死西安，导致粘成都攻略会得到「坐标全在西安且不报错」的行程。现支持 5 城 + 任意城市（`cities.py` + 城市识别 + `demo_spots.json` 高德真实坐标），84 个测试含坐标错配守卫 | — |
| ✅ C5 | ~~无分享长图~~ **已完成（2026-09-21）** | `22ac8f7` Canvas 手绘导竖版 PNG（含 AI 总评），**零依赖**；刻意不画地图（高德静态图会跨域污染画布，`toBlob` 抛 SecurityError） | — | — | ✅ 分享按钮在行程页，导出 PNG 到本地 |
| C6 | 软删除无清理策略 | 定期归档或提供「永久删除」 |

---

## 五、路线图（含依赖关系）

```
P0 解锁「能讲」
  A4 README 重写 ──┐
  A5 实验报告    ──┼──> 依赖 A2 复测数据 ──> 依赖 A1 CP-SAT 结果（gap 章节）
  A2 复测        ───┤
  A3 标注集扩充  ───┤
  A1 CP-SAT 对照 ───┘

P1 解锁「可信可用」（可与 P0 并行一部分）
  B7 依赖锁定 ─> B8 部署上线 ─> B3 任务淘汰（压测在部署环境做）
  B1 数据库 ──> B2 用户体系
  B4 XSS 修复 ─> B5 日志 ─> B6 重试     （独立，可穿插）

P2 加分
  C1 前端拆分（在加新页面前做，否则技术债滚大）
  C3 CI 增强、C2 接口测试、C4 多城市

P3 可选
  C5 分享长图、C6 归档策略

不做：多人协同、天气接入、支付、纯动效堆料
```

### 批次建议（每批 1~2 次会话，产出可验收）

**批次 1｜把已有资产变成可读结果（最高性价比）**
1. A4 README 重写（架构 + 量化结果 + 上手）
2. A5 实验报告初版（用现有 50 场景数据）
3. A2 复测（DeepSeek vs GLM 选型表，含延迟/成本/格式遵从性）
> 验收：外人只看 README 就能理解项目价值与技术判断。

**批次 2｜补算法深度**
4. A1 CP-SAT 对照 + gap 分析（含「何时用启发式、何时用精确解」的结论）
> 验收：50 场景 gap 表；能口头解释建模差异与取舍。

**批次 3｜补评测严谨性**
5. A3 标注集扩到 100~200 条 + F1 + 转人工率
> 验收：难例分布 + 指标 + 失败案例分析。

**批次 4｜工程化与上线**
6. B7 依赖锁定 → B1 数据库 → B2 用户体系 → B8 部署
> 验收：公网链接 + 两账号数据隔离演示。

**批次 5｜健壮性收口**
7. B4 XSS → B5 日志 → B6 重试 → B3 任务淘汰 + 压测
> 验收：压测报告 + 故障注入测试。

**批次 6｜前端与测试债**
8. C1 拆分、C2 接口测试、C3 CI 增强

---

## 六、面试资产包（现在就能用）

### 简历项目描述（一版草稿）

> **AI 行程规划系统**（Python / FastAPI / LLM / 组合优化，个人项目）
> 设计并实现从非结构化攻略到可执行多日行程的完整链路：LLM 结构化抽取 → 高德实体对齐 → 带时间窗与住宿锚点约束的路线调度 → 独立约束校验。
> · 自研启发式求解器（贪心构造 + 2-opt + 跨日搬运 + 多起点随机重启），50 组场景对照实验中 **100% 无冲突、0 预算违规**，通勤占比 0.131（朴素基线 0.235），与 OR-Tools CP-SAT 最优解 **gap 均值 −0.03%、≤3% 覆盖 100%**，单次求解 501ms（CP-SAT 均值 4.18s）
> · 实证「LLM 直接排期质量分接近（0.921 vs 0.915）但预算违规 8 次」，据此确立 LLM 理解 + 求解器合规的混合架构
> · 高德通勤矩阵三级缓存（进程内/磁盘/API）+ 0.35s 限频，重复求解 **API 调用降为 0**（首次 1717 次）
> · 实体对齐 F1 90%→**100%**：用「尾部子景点惩罚 + 主名权威性先验（跨字段证据检测景区更名）+ 候选集内 LLM 仲裁」修复真实失败案例；转人工率 20%→15%
> · **195 个单元测试** + GitHub Actions CI（五道门：单测 / 前端静态检查 / 后端五维健壮性审计 / jsdom 冒烟 / 无密钥 demo）；轻任务模型通道接入免费模型，后台评价生成零成本
> · 稳健性建模：1000 次蒙特卡洛给「按时走完的概率」+ 共同随机数定位风险点；**稳健排程**按用户指定的稳妥度自适应留缓冲重排（实测用 1 个景点换 92 个百分点确定性）

### 三个可讲的调试故事（都是真的）

1. **两套通勤口径不一致**：定点插入用 haversine 估算（兵马俑→永兴坊估 101 分钟），基准行程用高德真实数据（43 分钟），导致可行方案被判不可行 → 统一注入同一通勤函数。
2. **批量删除上限**：删 10 条成功、14 条报 500，定位到执行环境的安全删除守卫拦截物理删除 → 改为软删除（标记位），顺带获得误删可恢复能力。
3. **多轮指代记忆**：「设汉庭」→ 下一句「换到西安站附近」能正确指代住宿 → 靠编辑链路的 `chat_history` 跨任务传递。

### 预判追问与答案要点

| 追问 | 答题要点 |
|------|---------|
| 为什么用启发式不用精确解？ | 讲规模增长下的求解时间；讲 CP-SAT 对照实验的 gap 与耗时曲线 |
| 如何保证约束不被破坏？ | 求解器内每次插入都做时间线可行性校验 + 独立的 `constraint_check` 二次复核 |
| LLM 输出不稳定怎么办？ | 容错 JSON 解析 + 归一化 + 类型过滤 + 转人工（`needs_review`），并用 F1 量化 |
| 缓存怎么设计？ | 三级、命中率统计、限频 0.35s、方向敏感性（来回时长不同） |
| 为什么要独立校验？ | 求解器可能被优化步骤破坏不变量（曾出现 relocate 漏传 hotel 参数导致输出不可行） |
| 怎么做多用户？ | 讲表结构与隔离；讲内存任务淘汰与 TTL |
| 扩展新城市要改什么？ | 只换 demo 数据与 city 参数（已参数化）；讲对齐器的类型过滤可复用 |
| 你最大的技术取舍是什么？ | 「返回时间约束」让远线必须早去早回，宁可少排景点也不生成不可执行行程 |

---

## 七、命令备忘

```bash
# 启动
cd backend && python -m uvicorn main:app --port 8000
# 测试
cd backend && python -m pytest tests/ -q
# 排期对照实验（朴素 vs 求解器 vs LLM）
cd backend && python evaluation.py --n 50 --with-llm
# 对齐评测（F1 / 转人工率）
cd backend && python eval_aligner.py
```

---

## 八、五个现场问题的具体解法

审计发现的 5 个问题都在 B 级（影响「可信可用」），逐条给出做法、代价、验收。**建议在批次 4/5 集中处理，其中 B6 可以提前顺手做掉**。

| # | 问题 | 具体做法 | 代价 | 验收 |
|---|------|---------|------|------|
| B5 | 无日志体系 | ①各模块 `logging.getLogger(__name__)` ②`main.py` 启动时配 `dictConfig`（级别从环境变量读，默认 INFO，本地可 DEBUG）③请求中间件注入 `request_id`（`contextvars`），日志格式带 `%(request_id)s` ④高德/LLM 调用记录耗时与重试次数 ⑤不引第三方库，保持零依赖 | 低（约 80 行，1 次会话） | 一次规划从头到尾可按 `request_id` 串起日志 |
| B3 | 任务只增不减 | ①`TaskManager` 加容量上限（如 200）+ 淘汰最旧且 `status in (completed, failed)` 的任务 ②完成任务加 TTL（如 2 小时）惰性清理（每次 `create()` 时顺手扫一遍）③仍在运行的任务永不淘汰（避免打断）④落盘快照本来就独立于内存，淘汰不丢历史 | 低（约 40 行 + 测试） | 压测：连续创建 500 个任务后 `len(_tasks) ≤ 200`，内存平稳 |
| B4 | 前端 32 处 `innerHTML` 直插外部内容 | ①加 `const esc = s => String(s).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))` ②所有插入 LLM/高德来源文本的位置（景点名、desc、intro、评价、reply、changes）统一走 `esc()` ③纯静态模板保留 `innerHTML` ④补一条注入用例（景点名含 `<img onerror=...>`）作为手工验收 | 中（约 60 处替换，需逐个核对，1~2 次会话） | 注入用例不执行；功能无回归 |
| B6 | 无重试；`extractor` 无超时 | ①封装 `call_llm_with_retry()`：统一 `timeout=30`、最多 3 次、指数退避（1s/2s/4s）、只对可重试错误（超时/429/5xx）重试 ②高德同理包一层（已有 0.35s 限频，重试时保留）③`extractor` 补 timeout ④重试事件写进日志（与 B5 联动） | 低（约 50 行，可顺手做） | 用「断网 + 单个接口 500」注入测试：最终成功或明确失败，不挂起 |
| B7 | 依赖未锁版本、缺开发依赖 | ①拆 `backend/requirements.txt`（运行时：fastapi/uvicorn/pydantic/openai/httpx）与 `backend/requirements-dev.txt`（pytest/ruff/mypy）②用 `pip freeze` 或 `pip-tools` 生成锁定文件 ③CI 装锁定版 | 低 | 全新虚拟环境按文档一键起，版本完全一致 |
| B1/B2 | 无数据库、无用户体系 | ①SQLite + SQLAlchemy 2.x，三表：`users(id, token, created_at)` / `plans(id, user_id, city, params_json, result_json, deleted_at)` / `favorites(user_id, name, lat, lon, ...)` ②保留 JSON 快照做双写过渡，迁移脚本把 `data/plans/*.json` 导入 ③鉴权用最简令牌（`Authorization: Bearer`，登录接口发 token；不引 OAuth）④`solver`/`aligner` 不动，只换持久层 | 中高（2~3 次会话） | 两个账号数据完全隔离；重启服务数据不丢；迁移后 `/plans` 与新表一致 |
| B8 | 未部署 | ①`Dockerfile`（python:3.12-slim + 单 worker，SQLite 挂载卷）②`docker-compose.yml` 便于本地复现 ③部署到 Render（免费额度）或 Fly.io ④环境变量在平台侧配置，`.env` 不进镜像 ⑤README 补访问链接与冷启动说明 | 中（1~2 次会话，含账号注册） | 公网 URL 可打开并完成一次规划 |



## 九、优先级复审（2026-09-19 更新：N1 已完成）

**用户明确推迟部署**（理由：功能不够扎实、泛化未处理）→ 先补能力，再上线。这个判断是对的。

**N1 多城市泛化已完成（2026-09-19）**：城市中心表 + 城市识别 + 5 城真实数据 + 前端选择器与攻略入口 + 33 个新测试。
验证结果：成都攻略端到端跑通（识别城市 → 8 个景点全部落在成都真实坐标 → 2 天行程 8 个景点全部排入），西安路径回归正常。

**接下来的顺序（按「技术深度 × 面试可讲性」）**：

1. **N2 地理聚类分天** —— 构造前按地理位置聚类再分天。现在天与天之间没有聚类，可能出现「城东城西各排一半」。通勤占比 13.1% 有望进 10%，且能复用 `eval_gap.py` 框架做改前改后对照（**方法论的完整闭环：发现瓶颈 → 假设 → 改造 → 对照实验**）。
2. **A8 对齐器第二轮：子景点后缀** —— 本轮新发现：抽「成都大熊猫繁育研究基地」会对齐到「…-熊猫塔」、「杜甫草堂」→「…-杜陵村」。A7 的尾部惩罚只覆盖「长前缀+别名在尾部」，这类「别名+短后缀子点」没管，多城市场景下更常见。
3. **N3 行程稳健性模拟** —— 蒙特卡洛给停留/通勤加噪声，输出「按时完成概率 + 风险点」。把确定性优化升级为**鲁棒优化**，技术深度最高。
4. 门票/停留数据补全、C3 ruff/mypy、C2 接口测试。
5. B1/B2 数据库与用户体系（工程量较大）。

**时间线（对齐 2027 年 3 月开岗）**

| 时间 | 动作 |
|------|------|
| 2026-09 下旬 | N2 地理聚类分天 + A8 对齐器子景点（1~2 次会话） |
| 2026-10 | N3 稳健性模拟；B1/B2 数据库与用户体系；并行产出 A3 难例工厂 |
| 2026-11 | A3 复测、C2 接口测试、一次压测 |
| 2026-12 | **停止加功能**，面试资产定稿 + 刷题；部署（届时按 docs/deploy_freetier.md 选方案） |
| 2027-01~02 | 投日常实习练手 |
| 2027-03 | 暑期实习正式开岗 |

**一个被两份文档都低估的风险**：项目已 4,100 行、22 个模块、4 个量化实验，**远超「简历够用」的线**。
真正会让你挂面试的不是功能少，而是**你对自己项目的解释深度跟不上代码复杂度**。
所以此后每完成一项，必须加一道口述验收：合上电脑，用 3 分钟讲清「这块为什么这么做、替代方案是什么、代价是什么」——讲不出来就说明这任务白做。

---

## 十、更新记录

- 2026-09-18 首版：事实基线、五维评估、A/B/C 问题清单、六批路线图、面试资产包；补第八节「五个现场问题的具体解法」。
- 2026-09-18 深夜复审：A1~A7、B3~B7 已完成 → 刷新现状事实表与五维评分，新增第九节优先级复审（B8 → B1/B2 → A3 → C3/C2 的顺序与时间线）。
- 2026-09-23 文档漂移复审（重装系统后取回仓库 + 全量代码审计）：README 更新日志停在 v1.2，实际已到 v1.5 → 补记 13 个遗漏功能；现状事实表全量刷新（48 py / 7745 行、22 接口、195 测试、前端 2241 行、80 提交、25 城 254 景点、标注集 22 条）；C5 分享长图标完成；**新增「文档一致性」评估维度（5 分）**；C1 前端拆分因行数翻倍而优先级上调。
- 2026-09-23 另记：新会话第一件事应是「先对齐文档再动手」——本项目功能增长快于文档更新速度，每隔几周就要做一次口径核对。
